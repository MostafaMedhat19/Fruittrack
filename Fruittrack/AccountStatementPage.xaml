<Page x:Class="Fruittrack.AccountStatementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      FlowDirection="RightToLeft"
      FontFamily="Segoe UI"
      Title="كشف حساب"
      Background="#F5F5F5">

    <Grid Margin="15">
        <Grid.Resources>
            <!-- Shared Styles -->
            <Style TargetType="TextBlock" x:Key="HeaderText">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="Foreground" Value="#444"/>
            </Style>

            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="Background" Value="#2196F3"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="16,10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="MinWidth" Value="120"/>
            </Style>

            <Style TargetType="Border" x:Key="CardStyle">
                <Setter Property="Background" Value="White"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" 
                Style="{StaticResource CardStyle}"
                Padding="20" 
                Margin="0,0,0,15">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="كشف حساب" 
                               FontSize="24" 
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Foreground="#2196F3"/>
                    
                    <TextBlock Text="{Binding EntityName}" 
                               FontSize="18" 
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Foreground="#666"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        Content="رجوع" 
                        Command="{Binding GoBackCommand}" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#FF9800"/>
            </Grid>
        </Border>

        <!-- Summary Section -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}"
                Padding="20" 
                Margin="0,0,0,15">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="إجمالي المبالغ اللي ليه: " FontWeight="Bold" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding TotalCredit, StringFormat=C}" Foreground="#4CAF50" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="إجمالي المبالغ اللي عليه: " FontWeight="Bold" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding TotalDebit, StringFormat=C}" Foreground="#F44336" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="الصافي النهائي: " FontWeight="Bold" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding FinalBalance, StringFormat=C}" 
                               Foreground="{Binding FinalBalanceColor}" 
                               FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Transactions Section -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Grid.Row="0" 
                           Text="تفاصيل المعاملات" 
                           Margin="20,20,20,10"
                           FontSize="18" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Foreground="#2196F3"/>

                <!-- Transactions DataGrid -->
                <DataGrid Grid.Row="1"
                         ItemsSource="{Binding Transactions}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         IsReadOnly="True"
                         Margin="10,0,10,10"
                         GridLinesVisibility="None"
                         HeadersVisibility="Column"
                         AlternatingRowBackground="#F8F9FA"
                         RowBackground="White"
                         BorderThickness="0"
                         ColumnHeaderHeight="40"
                         RowHeight="40">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#444"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="التاريخ" 
                                            Binding="{Binding TransactionDate, StringFormat=dd/MM/yyyy}" 
                                            Width="120"/>
                        <DataGridTextColumn Header="نوع المعاملة" 
                                            Binding="{Binding TransactionType}" 
                                            Width="100"/>
                        <DataGridTextColumn Header="المبلغ" 
                                            Binding="{Binding Amount, StringFormat=C}" 
                                            Width="120"/>
                        <DataGridTextColumn Header="الملاحظات" 
                                            Binding="{Binding Notes}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="الرصيد الجاري" 
                                            Binding="{Binding RunningBalance, StringFormat=C}" 
                                            Width="120"/>
                    </DataGrid.Columns>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Grid Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page> 