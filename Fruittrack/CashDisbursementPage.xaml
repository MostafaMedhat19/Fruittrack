<Page x:Class="Fruittrack.CashDisbursementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      FlowDirection="RightToLeft"
      FontFamily="Segoe UI"
      Title="صرف نقدية"
      Background="#F5F5F5">

    <Grid Margin="15">
        <Grid.Resources>
            <!-- Shared Styles -->
            <Style TargetType="TextBlock" x:Key="HeaderText">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="Foreground" Value="#444"/>
            </Style>

            <Style TargetType="Control" x:Key="InputControl">
                <Setter Property="Padding" Value="12,10"/>
                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Height" Value="40"/>
            </Style>

            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="Background" Value="#2196F3"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="16,10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="MinWidth" Value="120"/>
            </Style>

            <Style TargetType="Border" x:Key="CardStyle">
                <Setter Property="Background" Value="White"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form Section -->
        <Border Grid.Row="0" 
                Style="{StaticResource CardStyle}"
                Padding="20" 
                Margin="0,0,0,15">

            <StackPanel Orientation="Vertical">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="صرف نقدية" 
                               FontSize="22" 
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Foreground="#2196F3"/>

                    <!-- Export Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="طباعة" 
                                Click="PrintButton_Click"
                                Background="#FF9800" Foreground="White"
                                Padding="12,8" FontWeight="SemiBold" BorderThickness="0"
                                MinWidth="100" Margin="5,0" Cursor="Hand"/>
                        <Button Content="PDF" 
                                Click="PdfButton_Click"
                                Background="#F44336" Foreground="White"
                                Padding="12,8" FontWeight="SemiBold" BorderThickness="0"
                                MinWidth="100" Margin="5,0" Cursor="Hand"/>
                        <Button Content="Excel" 
                                Click="ExcelButton_Click"
                                Background="#4CAF50" Foreground="White"
                                Padding="12,8" FontWeight="SemiBold" BorderThickness="0"
                                MinWidth="100" Margin="5,0" Cursor="Hand"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Entity Type -->
                    <!-- Entity Name -->
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,10">
                        <TextBlock Text="اسم الجهة" Style="{StaticResource HeaderText}"/>
                        <ComboBox x:Name="cmbEntityName"
               ItemsSource="{Binding EntityNames}"
               SelectedItem="{Binding SelectedEntityName, Mode=TwoWay}"
               IsEditable="True"
               Text="{Binding EntityName, UpdateSourceTrigger=PropertyChanged}"
               ToolTip="اختر أو اكتب اسم الجهة"
               Style="{StaticResource InputControl}"/>
                    </StackPanel>

                    <!-- Date -->
                    <StackPanel Grid.Column="2" Grid.Row="0" Margin="10,0,0,10">
                        <TextBlock Text="التاريخ" Style="{StaticResource HeaderText}"/>
                        <DatePicker SelectedDate="{Binding TransactionDate, UpdateSourceTrigger=PropertyChanged}" 
                 Style="{StaticResource InputControl}"/>
                    </StackPanel>

                    <!-- Amount Owed -->
                    <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,0,10,10">
                        <TextBlock Text="المبلغ اللي عليه" Style="{StaticResource HeaderText}"/>
                        <TextBox Text="{Binding Debit, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
              ToolTip="ادخل المبلغ المصروف"
              Style="{StaticResource InputControl}"/>
                    </StackPanel>

                    <!-- Amount Owned -->
                    <StackPanel Grid.Column="2" Grid.Row="2" Margin="10,0,0,10">
                        <TextBlock Text="المبلغ اللي له" Style="{StaticResource HeaderText}"/>
                        <TextBox Text="{Binding Credit, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
              ToolTip="ادخل المبلغ المستلم"
              Style="{StaticResource InputControl}"/>
                    </StackPanel>

                    <!-- Notes -->
                    <StackPanel Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="3" Margin="0,10,0,0">
                        <TextBlock Text="الملاحظات" Style="{StaticResource HeaderText}"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
              ToolTip="ادخل أي ملاحظات"
              Style="{StaticResource InputControl}"
              Height="80"
              TextWrapping="Wrap"
              AcceptsReturn="True"
              VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>

                    <!-- Buttons -->
                    <StackPanel Grid.Column="2" Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="صرف نقدية" 
                                Command="{Binding AddDisbursementCommand}" 
                                Style="{StaticResource PrimaryButton}"
                                Background="#4CAF50"
                                Margin="0,0,10,0"/>

                        <Button Content="كشف حساب" 
                                Command="{Binding ViewStatementCommand}" 
                                Style="{StaticResource PrimaryButton}"
                                Background="#FF9800"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Summary Table Section -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="ملخص المعاملات" 
                               Margin="0,10,10,0"
                               FontSize="20" 
                               FontWeight="Bold" 
                               HorizontalAlignment="Center"
                               Foreground="#2196F3"/>
                </StackPanel>

                <!-- Search Box -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <Grid Width="245" Height="35" Margin="0,0,15,10">
                 

                    </Grid>
                </StackPanel>

                <!-- Transactions DataGrid -->
                <DataGrid Grid.Row="1"
                         ItemsSource="{Binding FilteredTransactions}"
                         SelectedItem="{Binding SelectedTransaction}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         IsReadOnly="True"
                         Margin="10,0,10,10"
                         GridLinesVisibility="None"
                         HeadersVisibility="Column"
                         AlternatingRowBackground="#F8F9FA"
                         RowBackground="White"
                         BorderThickness="0"
                         ColumnHeaderHeight="40"
                         RowHeight="40">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="30,0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#444"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
     
                        <DataGridTextColumn Header="اسم الجهة" 
                                            Binding="{Binding EntityName}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="التاريخ" 
                                            Binding="{Binding TransactionDate, StringFormat=d}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="له كام" 
                                            Binding="{Binding Credit, StringFormat=C}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="عليه كام" 
                                            Binding="{Binding Debit, StringFormat=C}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="الصافي" 
                                            Binding="{Binding Balance, StringFormat=C}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="ملاحظات" 
                                            Binding="{Binding Notes}" 
                                            Width="*"/>
                        <DataGridTemplateColumn Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="حذف" 
                                            Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Background="#F44336"
                                            Foreground="White"
                                            Padding="10,5"
                                            Margin="5,0"
                                            FontWeight="SemiBold"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            ToolTip="حذف المعاملة"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Grid Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>

                <!-- Summary Totals -->
                <Border Grid.Row="2" 
                        Background="#F5F5F5" 
                        Padding="10" 
                        Margin="10,0,10,10"
                        CornerRadius="0,0,8,8"
                        BorderThickness="0,1,0,0"
                        BorderBrush="#E0E0E0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="إجمالي له كام: " FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding TotalCredit, StringFormat=C}" Foreground="#4CAF50" FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="إجمالي عليه كام: " FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding TotalDebit, StringFormat=C}" Foreground="#F44336" FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="الصافي: " FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding NetBalance, StringFormat=C}" 
                                       Foreground="{Binding NetBalanceColor}" 
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>